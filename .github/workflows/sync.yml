# File: .github/workflows/sync.yml
name: Sync fork

on:
  schedule:
  - cron: "0 * * * *" # Run the job every hour
  push:
    branches:
    - "*"
    paths:
    - .github/workflows/sync.yml

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: gh repo sync <NostalgicG>/<Yar-Harr> # Replace the placeholders within the < >
      env:
        GH_TOKEN: $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
    - uses: gautamkrishnar/keepalive-workflow@v1
